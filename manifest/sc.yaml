apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: demo-class  # Replace with a descriptive name (same as your PVC)
provisioner: kubernetes.io/hostPath  # Adjust if using a different provisioner

